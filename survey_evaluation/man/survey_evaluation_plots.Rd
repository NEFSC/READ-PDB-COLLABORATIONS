% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_bts_survey_evaluations.R
\name{survey_evaluation_plots}
\alias{survey_evaluation_plots}
\title{Evaluate and Summarize Bottom Trawl Survey Tows}
\usage{
survey_evaluation_plots(
  filepath.sv.area = NULL,
  filepath.spp.tow.map = NULL,
  filepath.mean.index = NULL,
  od = output.dir,
  sv.unit = "N",
  tsa = 0.0112,
  plot.h = 6.5,
  plot.w = 8,
  plot.nrow.pp = 4,
  plot.ncol.pp = 5,
  yaxis.max = NULL,
  plot.f = "png",
  fall.color = "#5588ddaa",
  spring.color = "#44cc77cc",
  summary.color = "#ffaa77bb",
  strata.color.set = rep(c("#4F2d03aa", "#FF9839", "#F01875", "#2C089799", "#000C7Dbc",
    "#98FF15", "#30CD13", "#1F890B", "#0F4B05", "#013f16aa", "#Fb906D", "#d6A85Daa",
    "#86B877", "#57BBB3", "#37B7EC", "#c9c9c9", "#A4C503", "#2F896B", "#082FB5cc",
    "#11111177", "#df99aa", "#FE83FD", "#B91CFE", "#150FD3", "#00024Bcc", "#D70500",
    "#FC6C9B", "#FfCD1C", "#65AF1E", "#085CF8"), 4)
)
}
\arguments{
\item{filepath.sv.area}{Character. Filepath to the CSV file containing survey strata names and areas. Defaults to \code{NULL}.}

\item{filepath.spp.tow.map}{Character. Filepath to the CSV file containing individual tow data, including catches and coordinates. Defaults to \code{NULL}.}

\item{filepath.mean.index}{Character. Filepath to the CSV file containing ADIOS stratified mean index estimates. Defaults to \code{NULL}.}

\item{od}{Character. The output directory path where generated plots and CSV summaries will be saved. Defaults to \code{output.dir}.}

\item{sv.unit}{Character. The survey unit to evaluate. Options are \code{'N'} for abundance (numbers/tow) or \code{'Kg'} for biomass (kg/tow). Defaults to \code{'N'}.}

\item{tsa}{Numeric. Tow swept area in square nautical miles (nm^2). Currently not utilized in calculations. Defaults to \code{0.0112}.}

\item{plot.h}{Numeric. Height of the output plots in inches. Defaults to \code{6.5}.}

\item{plot.w}{Numeric. Width of the output plots in inches. Defaults to \code{8}.}

\item{plot.nrow.pp}{Numeric. Number of rows per page for multi-page faceted plots. Defaults to \code{4}.}

\item{plot.ncol.pp}{Numeric. Number of columns per page for multi-page faceted plots. Defaults to \code{5}.}

\item{yaxis.max}{Numeric. Maximum value for the y-axis on stratum mean plots. If \code{NULL}, the maximum observed value is used. Defaults to \code{NULL}.}

\item{plot.f}{Character. The file format/device extension for the exported plots (e.g., \code{"png"}, \code{"pdf"}). Defaults to \code{"png"}.}

\item{fall.color}{Character. Hex color code with transparency for Fall survey data visualizations. Defaults to \code{'#5588ddaa'}.}

\item{spring.color}{Character. Hex color code with transparency for Spring survey data visualizations. Defaults to \code{'#44cc77cc'}.}

\item{summary.color}{Character. Hex color code with transparency for summarized visual components. Defaults to \code{'#ffaa77bb'}.}

\item{strata.color.set}{Character vector. A recycled vector of hex colors used to uniquely identify individual strata in plots.}
}
\value{
The function does not explicitly return an object in the R environment. Instead, it writes
diagnostic data tables (\code{.csv}) and visual trend plots (\code{.png} or specified format) directly to the target \code{od} path.
}
\description{
This function processes bottom trawl survey (BTS) data to calculate stratum-specific
means, proportions of stock area sampled, and total tow counts. It generates a comprehensive
suite of diagnostic plots (as PNGs or other formats) and CSV summaries to evaluate
survey design metrics across seasons (SPRING and FALL) and years.
Code developed by Liz Brooks (May 2026; upload to repo August 2026)
Contributions by Chris Legault (August 2026 (facet_wrap_paginate! and helpful checking for tripping hazards))
}
\details{
The function filters input data for \code{PURPOSE_CODE == 10} and limits seasons strictly to \code{"SPRING"} and \code{"FALL"}.
It automatically checks for the target output directory and creates it recursively if it does not exist.
If sampling is determined to be incomplete in a given year-season layer (sampled area < 100\%), it generates
an extra tracking plot mapping the divergence between the total area mean and the sampled area mean.
}
